const app = require('../app');
const { expect } = require('chai');
const supertest = require('supertest');

describe('app module', () => {
  describe('get apps', () => {
    it('should return a jason array of all apps', () => {
      return console.log('asdf');
    });
  });
});
